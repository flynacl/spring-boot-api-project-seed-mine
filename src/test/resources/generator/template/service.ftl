package ${targetPrePath}.service;

import ${basePackage}.core.Service;

import ${targetPrePath}.model.${modelNameUpperCamel};

/**
 * @author ${author}
 * @date ${date}
 */
public interface ${modelNameUpperCamel}Service extends Service<${modelNameUpperCamel}> {

}
